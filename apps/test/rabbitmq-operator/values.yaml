# destination:
#   namespace: "{{$metadata.tenant}}-ironic"

sourcesObject:
  main:
    repoURL: git@github.com:tombulled/k8s.git
    path: charts/rabbitmq-operator
    targetRevision: main
#     helm: ~
