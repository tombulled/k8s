{{- range $id, $_ := .Values.extraResources }}
{{- $data := tpl (. | toYaml) $.Values.common | fromYaml }}
---
{{ $data | toYaml }}
{{- end }}
---
{{ range $namespace := include "namespaces" $ | fromYamlArray }}
{{ range $id, $_ := $namespace.extraResources }}
{{- $_ := mustMergeOverwrite . (dict "metadata" (dict "namespace" $namespace.name)) }}
---
{{ . | toYaml }}
{{- end }}
{{- end }}
