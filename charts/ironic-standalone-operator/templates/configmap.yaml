apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ironic-standalone-operator.fullname" . }}-ironic-standalone-operator-config
  labels:
    {{- include "ironic-standalone-operator.labels" . | nindent 4 }}
