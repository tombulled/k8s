$enabled: true

namespaceDefaults:
  name: "{{$id}}"

namespaces:
  rabbitmq-system: {}

applicationDefaults:
  destination:
    namespace: rabbitmq-system

  sourcesObject:
    main:
      repoURL: git@github.com:tombulled/k8s.git
      path: charts/{{$id}}
      targetRevision: main
