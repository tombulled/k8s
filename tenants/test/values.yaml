$enabled: true

projects:
  main:
    annotations:
      message: "{{.message}}"
      food: "{{$food}}"
    destinations:
      - namespace: "*"
        name: in-cluster
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    namespaceResourceBlacklist: []
