$enabled: true

defaults:
  $role: some-role

projects:
  main:
    destinations:
      - namespace: "*"
        name: in-cluster
    clusterResourceWhitelist:
      - group: "*"
        kind: "*"
    namespaceResourceBlacklist: []


role:
  name: '{{ print "oidc:" $role }}'
