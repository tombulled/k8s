app-of-apps: # extra source so 'app-of-apps' prefix not needed?
  applicationSets:
    main:
      generators:
        - git:
            repoURL: git@github.com:tombulled/k8s.git
            revision: main
            files:
              - path: "apps/test/*.yaml" # TODO: template tenant name here
